# T6.22 domain-randomisation ranges live in src/marketsim/scenarios/randomise.py
# (WorldSettings is extra=forbid; a `randomise:` map needs a config.py field).
scale: 1.0
seed: 0
mode: professional          # game | professional
run_mode: lockstep          # lockstep | realtime
io_source: seed             # seed | bea
modules: []
events:
  damping: 0.7
  max_depth: 3
  max_concurrent: 4
  p_sum_cap: 0.9
  rumour_rate: 0.0
